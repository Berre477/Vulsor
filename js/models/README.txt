Put a NON-Draco-compressed Hail Mary ship model here named:  hail_mary.glb
Voyage auto-loads it and replaces the built-in ship. If it loads facing the
wrong way / wrong size, tune SHIP_MODEL in js/voyage.js (rotY, scale, offsets).
